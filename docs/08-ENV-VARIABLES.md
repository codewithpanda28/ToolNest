# ToolNest — Env Variables

> `.env.local` — not committed to git. Add `.env.local` to `.gitignore`.

## Planned (Phase 4+)
```
DATABASE_URL=postgres://...        # Railway Postgres
NEXTAUTH_SECRET=...                # Auth (Phase 5)
RAZORPAY_KEY_ID=...                # Payments (Phase 6)
RAZORPAY_KEY_SECRET=...
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```